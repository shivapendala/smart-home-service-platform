import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smarthome.service',
  appName: 'SmartHomePlatform',
  webDir: 'dist'
};

export default config;
